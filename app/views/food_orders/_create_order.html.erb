<%= form_tag payments_path do %>
  <%# <%= hidden_field_tag 'food_order_id', @food_order.id %>
  <div class="d-flex justify-content-center mb-3">
    <%= submit_tag 'Checkout', class: 'btn btn-primary' %>
  </div>
<% end %>
